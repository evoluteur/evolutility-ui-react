export const colors = { scheme: "category10" } as const;

export const labelColor = "#333333";
export const innerLabelColor = "white";

const chartOptions = {
  colors,
};

export default chartOptions;
